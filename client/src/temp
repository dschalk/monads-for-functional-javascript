
  const request = function request (x, prefix, group, mon) {
    socket.send(prefix + "," + O.pMgroup.x  + ',' + O.pMname.x + ',' + group);
    sources.WS.map(e => { console.log(e) };
      return mon.ret(e);
    )
  }  

  mM21.bnd(request, NN#$42, 'test'

